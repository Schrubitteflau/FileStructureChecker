/* Toutes les définitions déclarées dans "index.d.ts" correspondront aux fonctions
définies dans ce fichier "index.js" (situés dans le même dossier).
On peut ainsi détecter lors de la compilation la provenance de la signature de la fonction,
et donc déterminer les transformations éventuelles à effectuer sur celle-ci. */
function test() {}